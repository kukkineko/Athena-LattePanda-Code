// This file is automatically generated by Intel rclnodejs
//
// *** DO NOT EDIT directly
//
'use strict';
const ref = require('@rclnodejs/ref-napi');
const StructType = require('@rclnodejs/ref-struct-di')(ref);
const ArrayType = require('@rclnodejs/ref-array-di')(ref);
const primitiveTypes = require('../../rosidl_gen/primitive_types.js');
const deallocator = require('../../rosidl_gen/deallocator.js');
const translator = require('../../rosidl_gen/message_translator.js');
const DurationWrapper = require('../../generated/builtin_interfaces/builtin_interfaces__msg__Duration.js');
const AssistedTeleop_GoalRefStruct = StructType({
  time_allowance: DurationWrapper.refObjectType,
});
const AssistedTeleop_GoalRefArray = ArrayType(AssistedTeleop_GoalRefStruct);
const AssistedTeleop_GoalRefStructArray = StructType({
  data: AssistedTeleop_GoalRefArray,
  size: ref.types.size_t,
  capacity: ref.types.size_t
});
// Define the wrapper class.
class AssistedTeleop_GoalWrapper {
  constructor(other) {
    this._wrapperFields = {};
    if (typeof other === 'object' && other._refObject) {
      this._refObject = new AssistedTeleop_GoalRefStruct(other._refObject.toObject());
      this._wrapperFields.time_allowance =  new DurationWrapper(other._wrapperFields.time_allowance);
    } else if (typeof other !== 'undefined') {
      this._initMembers();
      translator.constructFromPlanObject(this, other);
    } else {
      this._initMembers();
    }
    this.freeze();
  }
  _initMembers() {
    this._refObject = new AssistedTeleop_GoalRefStruct();
    this._wrapperFields.time_allowance = new DurationWrapper();
  }
  static createFromRefObject(refObject) {
    let self = new AssistedTeleop_GoalWrapper();
    self.copyRefObject(refObject);
    return self;
  }
  static createArray() {
    return new AssistedTeleop_GoalArrayWrapper;
  }
  static get ArrayType() {
    return AssistedTeleop_GoalArrayWrapper;
  }
  static get refObjectArrayType() {
    return AssistedTeleop_GoalRefStructArray
  }
  static get refObjectType() {
    return AssistedTeleop_GoalRefStruct;
  }
  toRawROS() {
    this.freeze(true);
    return this._refObject.ref();
  }
  freeze(own = false, checkConsistency = false) {
    if (checkConsistency) {
    }
    this._wrapperFields.time_allowance.freeze(own, checkConsistency);
    this._refObject.time_allowance = this._wrapperFields.time_allowance.refObject;
  }
  serialize() {
    this.freeze(false, true);
    return this._refObject.ref();
  }
  deserialize(refObject) {
    this._wrapperFields.time_allowance.copyRefObject(refObject.time_allowance);
  }
  toPlainObject(enableTypedArray) {
    return translator.toPlainObject(this, enableTypedArray);
  }
  static freeStruct(refObject) {
    DurationWrapper.freeStruct(refObject.time_allowance);
  }
  static destoryRawROS(msg) {
    AssistedTeleop_GoalWrapper.freeStruct(msg.refObject);
  }
  static type() {
    return {pkgName: 'nav2_msgs', subFolder: 'action', interfaceName: 'AssistedTeleop_Goal'};
  }
  static isPrimitive() {
    return false;
  }
  static get isROSArray() {
    return false;
  }
  get refObject() {
    return this._refObject;
  }
  get time_allowance() {
    return this._wrapperFields.time_allowance;
  }
  set time_allowance(value) {
    if (value instanceof DurationWrapper) {
      this._wrapperFields.time_allowance.copy(value);
    } else {
      this._wrapperFields.time_allowance.copy(new DurationWrapper(value));
    }
  }
  copyRefObject(refObject) {
    this._refObject = new AssistedTeleop_GoalRefStruct(refObject.toObject());
    this._wrapperFields.time_allowance.copyRefObject(this._refObject.time_allowance);
  }
  copy(other) {
    this._refObject = new AssistedTeleop_GoalRefStruct(other._refObject.toObject());
    this._wrapperFields.time_allowance.copy(other._wrapperFields.time_allowance);
  }
  static get classType() {
    return AssistedTeleop_GoalWrapper;
  }
  static get ROSMessageDef() {
    return {"constants":[],"fields":[{"name":"time_allowance","type":{"isArray":false,"arraySize":null,"isUpperBound":false,"isDynamicArray":false,"isFixedSizeArray":false,"pkgName":"builtin_interfaces","type":"Duration","stringUpperBound":null,"isPrimitiveType":false},"default_value":null}],"baseType":{"pkgName":"nav2_msgs","type":"AssistedTeleop_Goal","stringUpperBound":null,"isPrimitiveType":false},"msgName":"AssistedTeleop_Goal"};
  }
  hasMember(name) {
    let memberNames = ["time_allowance"];
    return memberNames.indexOf(name) !== -1;
  }
}
// Define the wrapper of array class.
class AssistedTeleop_GoalArrayWrapper {
  constructor(size = 0) {
    this._resize(size);
  }
  toRawROS() {
    return this._refObject.ref();
  }
  fill(values) {
    const length = values.length;
    this._resize(length);
    values.forEach((value, index) => {
      if (value instanceof AssistedTeleop_GoalWrapper) {
        this._wrappers[index].copy(value);
      } else {
        this._wrappers[index] = new AssistedTeleop_GoalWrapper(value);
      }
    });
  }
  // Put all data currently stored in `this._wrappers` into `this._refObject`
  freeze(own) {
    this._wrappers.forEach((wrapper, index) => {
      wrapper.freeze(own);
      this._refArray[index] = wrapper.refObject;
    });
    this._refObject.size = this._wrappers.length;
    this._refObject.capacity = this._wrappers.length;
    if (this._refObject.capacity === 0) {
      this._refObject.data = null
    } else {
    this._refObject.data = this._refArray.buffer;
    }
  }
  get refObject() {
    return this._refObject;
  }
  get data() {
    return this._wrappers;
  }
  get size() {
    return this._wrappers.length;
  }
  set size(value) {
    if (typeof value != 'number') {
      throw new TypeError('Invalid argument: should provide a number to AssistedTeleop_GoalArrayWrapper.size setter');
      return;
    }
    return this._resize(value);
  }
  get capacity() {
    return this._wrappers.length;
  }
  set capacity(value) {
    if (typeof value != 'number') {
      throw new TypeError('Invalid argument: should provide a number to AssistedTeleop_GoalArrayWrapper.capacity setter');
    }
    return this._resize(value);
  }
  get refObject() {
    return this._refObject;
  }
  _resize(size) {
    if (size < 0) {
      throw new RangeError('Invalid argument: should provide a positive number');
      return;
    }
    this._refArray = new AssistedTeleop_GoalRefArray(size);
    this._refObject = new AssistedTeleop_GoalRefStructArray();
    this._refObject.size = size;
    this._refObject.capacity = size;
    this._wrappers = new Array();
    for (let i = 0; i < size; i++) {
      this._wrappers.push(new AssistedTeleop_GoalWrapper());
    }
  }
  // Copy all data from `this._refObject` into `this._wrappers`
  copyRefObject(refObject) {
    this._refObject = refObject;
    let refObjectArray = this._refObject.data;
    refObjectArray.length = this._refObject.size;
    this._resize(this._refObject.size);
    for (let index = 0; index < this._refObject.size; index++) {
      this._wrappers[index].copyRefObject(refObjectArray[index]);
    }
  }
  copy(other) {
    if (! (other instanceof AssistedTeleop_GoalArrayWrapper)) {
      throw new TypeError('Invalid argument: should provide "AssistedTeleop_GoalArrayWrapper".');
    }
    this._resize(other.size);
    // Array deep copy
    other._wrappers.forEach((wrapper, index) => {
      this._wrappers[index].copy(wrapper);
    });
  }
  static freeArray(refObject) {
    let refObjectArray = refObject.data;
    refObjectArray.length = refObject.size;
    for (let index = 0; index < refObject.size; index++) {
      AssistedTeleop_GoalWrapper.freeStruct(refObjectArray[index]);
    }
  }
  static get elementType() {
    return AssistedTeleop_GoalWrapper;
  }
  static get isROSArray() {
    return true;
  }
  static get useTypedArray() {
    return false;
  }
  get classType() {
    return AssistedTeleop_GoalArrayWrapper;
  }
}
module.exports = AssistedTeleop_GoalWrapper;
/*
 * The following is the original spec object coming from parser:
{
  "constants": [],
  "fields": [
    {
      "name": "time_allowance",
      "type": {
        "isArray": false,
        "arraySize": null,
        "isUpperBound": false,
        "isDynamicArray": false,
        "isFixedSizeArray": false,
        "pkgName": "builtin_interfaces",
        "type": "Duration",
        "stringUpperBound": null,
        "isPrimitiveType": false
      },
      "default_value": null
    }
  ],
  "baseType": {
    "pkgName": "nav2_msgs",
    "type": "AssistedTeleop_Goal",
    "stringUpperBound": null,
    "isPrimitiveType": false
  },
  "msgName": "AssistedTeleop_Goal"
}
*/
